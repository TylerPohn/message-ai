# Claude Code Rules

## String Management
Instead of adding hard coded strings, check `locales/translations.ts` for the string you need and add it in all languages if not present.

## Git rules
You are not allowed to run git commands. 
